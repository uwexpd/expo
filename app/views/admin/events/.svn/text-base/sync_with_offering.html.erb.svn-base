<h1>Sync with Offering</h1>
<p>This page allows you to add applications from the <%= @event.offering.title %> as invitees for this event. It will also uninvite any applications in the "cancelled" status.</p>

<% form_tag do %>
	
	<dl class="inline-definitions">

		<dt>Applications</dt>
		<dd>Invite applications that are in the following status:
			<br>
			<%= select_tag(:status, options_from_collection_for_select(@event.offering.statuses, :application_status_name, :private_title), :prompt => true) %>
			<p class="caption">Note that cancelled applications are automatically uninvited.</p>
			</dd>

		<dt>Time</dt>
		<dd>Add them to the following time slot for this event:
			<br>
			<%= select_tag(:event_time, options_from_collection_for_select(@event.times, :id, :time_detail)) %>
			</dd>

		<dt></dt>
		<dd><%= submit_tag "Sync" %></dd>

	</dl>
	
<% end -%>
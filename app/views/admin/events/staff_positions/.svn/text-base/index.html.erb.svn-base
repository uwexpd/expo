<h1>Staff Positions
	<span class="light">&raquo; <%=h @event.title %></span>
	<span class="light right"><%= link_to "add new", new_event_staff_position_path(@event) %></span></h1>

<p>Each event can have many staff positions for which people can volunteer. Each position can have multiple times or "shifts." Use this page to manage these positions and shifts.</p>

<table>
	<tr>
		<th>Title</th>
		<th>Shifts</th>
		<th>Require all shifts?</th>
		<th>Functions</th>
	</tr>

<% for staff_position in @staff_positions %>
<tr>
	<td><%=h staff_position.title %></td>
	<td><%= link_to staff_position.shifts.size, event_staff_position_shifts_path(@event, staff_position) %></td>
	<td><%= staff_position.require_all_shifts? ? "Yes" : "No" %></td>
	<td><%= link_to 'Show', event_staff_position_path(@event, staff_position) %>
		| <%= link_to 'Edit', edit_event_staff_position_path(@event, staff_position) %>
		| <%= link_to 'Delete', event_staff_position_path(@event, staff_position), :confirm => 'Are you sure?', :method => :delete %>
		| <%= link_to 'Nametags', staff_position_event_nametags_path(@event, :id => staff_position) %>
	</td>
</tr>
<% end %>

<% if @staff_positions.empty? %>
<tr class="empty">
	<td colspan=4>No staff positions defined. <%= link_to "Create one", new_event_staff_position_path(@event) %>.</td>
</tr>
<% end -%>

</table>
<h1>Committee Manager
	<span class="light">&raquo; <%= @committee.name %></span>
	<span class="light right"><%= link_to "Edit", edit_admin_committee_url(@committee), :class => 'edit button' %></span></h1>

<ul class="welcome-links">
	
	<li id="people"><%= link_to "Committee Members", admin_committee_members_path(@committee) %>
		<br>There are currently <b><%= @committee.members.size %> members</b> in the committee.
		</li>
		
	<li id="calendar"><%= link_to "Active Quarters", admin_committee_quarters_path(@committee) %>
		<br>View the quarters that this committee is active.
		<br><small>
		<%= @committee.committee_quarters.sort.collect{|q| link_to q.title, admin_committee_quarter_path(@committee, q)}.join(", ") %>
		</small>
		</li>

	<li id="meetings"><%= link_to @committee.meetings_title_display, admin_committee_meetings_path(@committee) %>
		<br>View the <%= @committee.meetings_title_display(true) %> that have been arranged for this committee.
		<br><small>
		<%= @committee.meetings.future.sort.collect{|m| link_to m.title, admin_committee_meeting_path(@committee, m)}.join(", ") %>
		</small>
		</li>

	<li id="edit"><%= link_to "Edit Committee Details", edit_admin_committee_path(@committee) %>
		<br>Edit the details of this committee, including the text that is displayed on the committee member interface.
		</li>


<br>

<% sidebar :add_member, :class => 'add_new' %>
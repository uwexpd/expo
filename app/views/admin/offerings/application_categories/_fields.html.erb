<dl class="inline-definitions">
	
	<dt><%= f.label :application_category_id, "Category" %></dt>
	<dd><%= f.collection_select :application_category_id, ApplicationCategory.all.sort, :id, :title, :prompt => true %></dd>
	
	<dt><%= f.label :offering_application_type_id, "Type" %></dt>
	<dd><%= f.collection_select :offering_application_type_id, @offering.application_types.all.sort, :id, :title, :prompt => true %>
			<small>or <%= link_to "Add an Appication Type to the Offering", 
						new_offering_application_type_path(:offering_id => @offering, :popup_layout => true, 
						:dom_id_to_update => 'application_category_offering_application_type_id'), 
						:popup => ['new_window', 'scrollbars=yes,height=700,width=800'] %></small></dd>
	
	<dt><%= f.label :other_option %></dt>
	<dd><%= f.check_box :other_option %> This option acts as an "other" option where users can type in a custom answer.</dd>
	
	<dt><%= f.label :sequence %></dt>
	<dd><%= f.text_field :sequence %></dd>

</dl>
<h1>Stop <%= @unit.name %> Emails</h1>

<% unless @error %>
<div>
  <h2>You are no longer set as an active tutor with <%= @unit.name %>.</h2>
  <h3>If this was a mistake go to the <%= link_to "Riverways Search", pipeline_base_path %> page and you will be set as an active <%= @unit.name %> tutor again.</h3>
</div>
<% else %>
  <h2>Sorry, something went wrong. Go to the <%= link_to "#{@unit.name} Search", pipeline_base_path %> page.</h2>
<% end %>